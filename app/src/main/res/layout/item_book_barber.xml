<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_barber"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="20dp">

    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp">

        <LinearLayout
            android:id="@+id/barber_linier"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp"
            android:background="@drawable/barber_item_background">

            <ImageView
                android:id="@+id/barber_image"
                android:layout_width="70dp"
                android:layout_height="90dp"
                android:contentDescription="@string/our_barber"
                android:scaleType="centerCrop"
                android:src="@drawable/logo" />

            <TextView
                android:id="@+id/barber"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textColor="@color/barber_text_selector"
                android:fontFamily="@font/poppins_bold"
                android:layout_marginTop="7dp"
                android:gravity="center"
                android:textAlignment="center"
                android:text="@string/service_menu" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</FrameLayout>